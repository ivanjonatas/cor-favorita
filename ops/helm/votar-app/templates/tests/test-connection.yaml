apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "votar-app.fullname" . }}-test-connection"
  labels:
    {{- include "votar-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['votar-api-service:8001/health']
  restartPolicy: Never
