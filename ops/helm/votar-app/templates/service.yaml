apiVersion: v1
kind: Service
metadata:
  name: {{ include "votar-app.fullname" . }}
  labels:
    {{- include "votar-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "votar-app.selectorLabels" . | nindent 4 }}
